import 'package:flutter/material.dart';

class AppPallete {
  // Hijau modern (kesehatan, profesional, menenangkan)
  static const primary = Color(0xFF2E7D32);

  // Secondary menyesuaikan (hijau muda segar)
  static const secondary = Color(0xFF66BB6A);

  // Warna netral
  static const white = Colors.white;
  static const black = Colors.black87;
  static const gray = Color(0xFF9F9FB5);

  // Opsional tambahan untuk konsistensi
  static const background = Color(0xFFF5F5F5); // abu muda untuk background
  static const accent = Color(0xFF388E3C); // aksen hijau
}
